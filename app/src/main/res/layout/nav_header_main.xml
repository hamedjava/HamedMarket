<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/header_main_background"
    android:padding="12dp">

    <ImageView
        android:id="@+id/img_user_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="32dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_user_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="HAMED"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:textAlignment="textStart"
        app:layout_constraintStart_toStartOf="@+id/img_user_logo"
        app:layout_constraintTop_toBottomOf="@+id/img_user_logo" />

    <TextView
        android:id="@+id/tv_user_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="hamedpouyanfar1987@gmail.com"
        android:textSize="16sp"
        android:textColor="@color/white"
        android:textAlignment="textStart"
        app:layout_constraintStart_toStartOf="@+id/tv_user_username"
        app:layout_constraintTop_toBottomOf="@+id/tv_user_username" />

</androidx.constraintlayout.widget.ConstraintLayout>